export interface IUser {
    id: number | null;
    username: string;
    password: string;
}

export interface IRegistro {
    id: number | null;
    nombre: string;
    observacionLateral: string;
    observacionMotor: string;
}
